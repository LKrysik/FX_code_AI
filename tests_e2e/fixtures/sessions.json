{
  "short_collection": {
    "symbols": ["BTC_USDT"],
    "session_type": "collect",
    "config": {
      "data_collection": {
        "duration": "30s"
      }
    }
  },
  "multi_symbol_collection": {
    "symbols": ["BTC_USDT", "ETH_USDT"],
    "session_type": "collect",
    "config": {
      "data_collection": {
        "duration": "1m"
      }
    }
  },
  "backtest_session": {
    "symbols": ["BTC_USDT"],
    "session_type": "backtest",
    "config": {
      "session_id": "<data_collection_session_id_placeholder>",
      "acceleration_factor": 10.0,
      "_comment": "session_id must reference a valid data collection session from QuestDB. Use GET /api/data-collection/sessions to list available sessions. This fixture is a template only - tests must replace session_id with actual session ID."
    }
  }
}
